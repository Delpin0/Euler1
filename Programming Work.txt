If we list all the natural numbers below 10 that are multiples of 3 or 5, we get 3, 5, 6 and 9. The sum of these multiples is 23.

Find the sum of all the multiples of 3 or 5 below 1000.

import math?

var x int
var sum int = 0
var total, check float32 = 0, 0

for i := 3; i <= 1000; i++ {
 total = x
 
 for x >= 3 do
   x=x/3
   if x != int goto end
   if x==1
     sum = sum+x
 
 for x >= 5 do 
   total = x
   x=x/5
   if x != into goto end
   if x==1
     sum = sum+x
   

   
   check := int x